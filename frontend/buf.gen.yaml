# ConnectRPC code generation for the Flutter client.
#
# Intentionally a placeholder during the skeleton phase. Generated Dart
# stubs will land in `lib/gen/` once the backend's `proto/` directory is
# in place. To re-enable, install the dart connect plugin and run
# `buf generate` from this directory.
version: v2
plugins: []
